{
  "name": "my-app",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --coverage",
    "lint": "eslint .",
    "build": "mkdir -p dist && cp -r src/* dist/"
  },
  "devDependencies": {
    "eslint": "^8.55.0",
    "jest": "^29.7.0"
  },
  "jest": {
    "collectCoverage": true,
    "coverageReporters": ["text", "lcov", "cobertura"]
  }
}
